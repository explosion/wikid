trigger:
    batch: true
    branches:
        include:
            - 'main'

variables:
    WASABI_LOG_FRIENDLY: 1

jobs:
    - job: 'Test'
      strategy:
          matrix:
              Python37Linux:
                  imageName: 'ubuntu-latest'
                  python.version: '3.7'
              Python37Windows:
                  imageName: 'windows-latest'
                  python.version: '3.7'
          maxParallel: 4
      pool:
          vmImage: $(imageName)

      steps:
          - task: UsePythonVersion@0
            inputs:
                versionSpec: '$(python.version)'
                architecture: 'x64'

          - script: |
                pip install pytest
                pip install wheel
            displayName: 'Install dependencies'

          - task: Bash@3
            inputs:
              filePath: 'python -m pytest .'